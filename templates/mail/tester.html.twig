{% extends 'default/base.html.twig' %}

{% block body %}
    <h1>Tester le système de mail</h1>

    <div class="inputrond submitdyn">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    </div>
    
{% endblock %}
